import { AboutSection, HeroSection, WorksSection } from '@/widgets/mainPageWidgets'

export default function Home() {
  return (
    <main>
      <HeroSection />
      <AboutSection />
      <WorksSection />
    </main>
  )
}
